# 服务器启动说明

## 问题诊断
您遇到的问题是服务器无法正常访问，这通常是由于以下原因：

1. **端口冲突**：多个 Next.js 进程同时运行
2. **缓存问题**：.next 目录的锁文件导致无法启动
3. **防火墙阻止**：Windows 防火墙可能阻止了 Node.js

## 解决方案

### 方案 1：使用批处理文件（推荐）
1. **双击运行** `start-app.bat` 文件
2. 这个文件会自动清理所有冲突的进程和缓存
3. 然后重新启动服务器

### 方案 2：使用命令行
打开命令提示符（以管理员身份运行），执行：
```cmd
cd "D:\桌面\Claude Code Study Test\social-insurance-calculator"
npm run dev:clean
```

### 方案 3：手动清理
1. 打开任务管理器（Ctrl+Shift+Esc）
2. 结束所有 node.exe 进程
3. 删除项目中的 .next 文件夹
4. 重新运行 `npm run dev`

## 访问地址
启动成功后，尝试以下地址：

1. **http://localhost:3000** （如果端口空闲）
2. **http://localhost:3001** （备用端口）
3. **http://localhost:3002** （备用端口）
4. **http://192.168.0.104:3000** （网络访问）
5. **http://192.168.0.104:3001** （网络访问）
6. **http://192.168.0.104:3002** （网络访问）

## 防火墙设置
如果仍然无法访问，请：
1. 打开 Windows 安全中心
2. 点击"防火墙和网络保护"
3. 选择"允许应用通过防火墙"
4. 添加 Node.js（通常在 C:\Program Files\nodejs\node.exe）

## 测试项目
启动成功后，您可以使用 `test-data.xlsx` 文件测试所有功能。